\PassOptionsToPackage{no-math}{fontspec}
\documentclass[a4paper,fontset=none]{ctexart}
\usepackage{graphicx,siunitx,subcaption,amssymb,amsmath,fixdif,tabularray,minted}
\sisetup{mode=text,per-mode=symbol}
\RenewDocumentCommand{\theFancyVerbLine}{}{\ttfamily{\arabic{FancyVerbLine}}}

\usepackage[left=3.17cm,right=3.17cm,top=2.54cm,bottom=2.54cm]{geometry}

\setCJKmainfont{Source Han Serif SC}[
    BoldFont       = Source Han Serif SC Bold,
    ItalicFont     = STKaiti,
    BoldItalicFont = STKaiti
]
\setCJKmonofont{Source Han Sans SC Light}
\setCJKsansfont{Source Han Sans SC Light}[
    BoldFont       = Source Han Sans SC Normal,
    ItalicFont     = Source Han Sans SC ExtraLight,
    BoldItalicFont = Source Han Sans SC Light
]
\setmainfont{EB Garamond}
\setmonofont{Source Code Pro}
\setsansfont{Source Sans 3 Light}[
    BoldFont       = Source Sans 3 Medium,
    ItalicFont     = Source Sans 3 Light Italic,
    BoldItalicFont = Source Sans 3 Medium Italic
]
\usepackage[colorlinks,linkcolor=blue,citecolor=red]{hyperref}
\NewDocumentCommand{\inlinec}{}{\mintinline{c}}
\NewDocumentCommand{\inlinerust}{}{\mintinline{rust}}
\title{关于 Xenon（ATC-X）内部实现的注释}
\author{REDACTED}
\date{\today}
\begin{document}
    \maketitle
    \section{}
    \section{组成部分}
    Xenon 包含 3 个部分：
    \begin{enumerate}
        \item 预处理器；
        \item 前端；
        \item 后端。
    \end{enumerate}
    \section{预处理器}
    按照顺序，预处理器负责：
    \begin{enumerate}
        \item 将单独的 \inlinec{\r}，或 \inlinec{\r\n} 替换为 \inlinec{\n}。
        \item 对于所有的以反斜杠结尾的行，将其与下一行连在一起. 例如：
        \item 删除所有的注释。
    \end{enumerate}
    以上三个步骤各通过一个自动机完成。自动机被组织成生成器的形式（使用 create generator）。
    \section{前端}
    前端负责将不包含注释的 SysY 源代码翻译为 KoopaIR。前端的处理步骤为：
    \subsection{第一次解析}
    使用 create pest 将代码处理为语法树。注意，这里语法树使用的是 pest 中 \inlinerust{Pairs<Rule>} 的形式。

    使用 pest 的原因是 pest 基于解析表达式文法，可以轻松绕过悬垂 else 问题。

    注意，只使用 pest parser 难以处理运算符优先级。在 pest parser 的基础上，表达式又会经过 \inlinerust{PrattParser<Rule>} 的解析。
    \subsection{第二次解析}
    遍历上述 \inlinerust{Pairs<Rule>} 的语法树，转为我定义的抽象语法树。这一步使用大量的递归函数。
    \subsection{语义检查}
    遍历抽象语法树，进行语义检查和常量表达式计算。这一步使用大量的递归函数。

    实际实现中，第二次解析和语义检查是合并在一起的。除此之外，还会进行以下两个化简：
    \begin{enumerate}
        \item 表达式化简，如 \inlinec{w = (x - x) * (y + z);} 会直接化简为 \inlinec{w = 0;}
        \item （简易的）不可达语句消除。任意 \inlinec{return} 后的语句，或是循环内 \inlinec{break} / \inlinec{continue}
        后的语句，都会直接忽略。
    \end{enumerate}

    为了生成 IR 时更加方便，还会进行两个操作：
    \begin{enumerate}
        \item 在初始化列表中填充 0；
        \item 名字重整。
    \end{enumerate}

    名字重整是这样的操作：代码
    \begin{minted}[linenos,frame=single]{c}
const int x[1] = {};
int main()
{
    int x = x[0];
    int y = x + 1;
    {
        int x = x;
        int z = y + x;
    }
    return x;
}
    \end{minted}
    被重整化为：
    \begin{minted}[linenos,frame=single]{c}
const int _Ax[1] = {};
int main()
{
    int _Ix = _Ax[0];
    int _Iy = _Ix + 1;
    {
        int _I_Ix = _Ix;
        int _Iz = _Iy + _I_Ix;
    }
    return _Ix;
}
    \end{minted}
    考虑到 C 标准中将“下划线+大写字母”开头的标识符视为保留名，这样做是完全合理的。
    \subsection{生成 IR}
    前端直接生成文本形式的 IR。同样地，这一步使用大量的递归函数。

    SysY 中的表达式被进一步分为三类：
    \begin{enumerate}
        \item 左值表达式，这种表达式计算的值是一个内存地址，如语句 \inlinec{x = 1;} 中的表达式 \inlinec{x} 是左值表达式；
        鉴于 Xenon 对 SysY 做了语法扩展，语句 \inlinec{(x = y) = z;} 中的表达式 \inlinec{x} 和 \inlinec{x = y} 都是左值表达式。
        \item 右值表达式，这种表达式计算的值只是一个值。
        \item 弃值表达式，这种表达式计算的值会被舍弃。如语句 \inlinec{x = 1;} 中的 \inlinec{x = 1} 是弃值表达式，这个语句只会执行赋值的
        副作用而不产生值；
    \end{enumerate}

    一个综合例子。语句 \inlinec{(x = f() + x) = (z = w);} 被翻译为 IR：
    \begin{minted}[linenos,frame=single]{c}
%5 = load %_Iw
store %5, %_Iz
call @f()
store %5, %_Ix
    \end{minted}
    在这之中，表达式 \inlinec{z = w} 是右值表达式，\inlinec{x = f() + x} 是左值表达式，而 \inlinec{(x = f() + x) = (z = w)} 整体是弃值表达式。
    上面的 IR 是优化过的，优化逻辑为：如果一个赋值表达式的值类别是左值，则这个赋值表达式等号右侧的表达式应该视为弃值而不是右值。未优化的版本为：
    \begin{minted}[linenos,frame=single]{c}
%5 = load %_Iw
store %5, %_Iz
%7 = call @f()
%8 = load %_Ix
%6 = add %7, %8
store %6, %_Ix
store %5, %_Ix
    \end{minted}
    三类表达式都有专门的函数处理。处理左值表达式和右值表达式的函数返回类型为 \inlinerust{(String, String)}，分别为计算这个表达式的过程和表达式的 ID。
    仍然以上述 \inlinec{(x = f() + x) = (z = w);} 语句为例。计算右值表达式 \inlinerust{(z = w)} 的过程为：
    \begin{minted}[linenos,frame=single]{c}
%5 = load %_Iw
store %5, %_Iz
    \end{minted}
    而这个表达式的 ID 是 \inlinec{%5}。
\end{document}
